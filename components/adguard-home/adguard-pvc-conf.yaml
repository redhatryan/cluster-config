apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-pvc-conf
  namespace: adguard
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: REPLACE
  volumeName: adguard-conf-pv